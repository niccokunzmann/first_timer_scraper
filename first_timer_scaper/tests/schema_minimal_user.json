{
  "$schema": "http://json-schema.org/draft-04/schema",
  "type" : "object",
  "format": "minimal",
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[^/#]+$"
    }
  },
  "required": ["name"]
}